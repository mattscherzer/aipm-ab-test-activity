[project]
name = "aipm-ab-test-activity"
version = "0.1.0"
description = "A/B test activity — Meridian homepage dek experiment (planning, synthetic data, analysis)"
requires-python = ">=3.12,<3.15"
dependencies = [
    "pandas>=3.0",
    "matplotlib>=3.10",
    "jupyterlab>=4.4",
    "ipykernel>=7.0",
]

[tool.uv]
package = false
